{% extends 'base_w_header.html.twig' %}

{% set modal = {
    id: 'deleteConfigurationModal',
    title: 'Delete Configuration',
    body: 'Are you sure you want to delete this configuration?',
    confirmButton: {
        label: 'Delete',
        class: 'btn-danger'
    },
    cancelButton: {
        label: 'Cancel',
        class: 'btn-secondary'
    }
} %}

{% block title %}
    Configurations
{% endblock %}

{% block body %}
    <div {{ turbo_stream_listen('App\\Entity\\Configuration') }}></div>

    <style>
        table {
            table-layout: fixed;
            min-width: 70rem;
        }

        .pagination {
            margin: 0;
        }
    </style>
    <div class="container">
        <h1>Configurations</h1>
        {{ component('AlertContainer') }}
        <div class="row">
            <turbo-frame id="configurationsTable">
            <div class="col-md-12">
                <table class="table table-striped">
                    <colgroup>
                        <col style="width: 10%">
                        <col style="width: 25%">
                        <col style="width: 10%">
                        <col style="width: 10%">
                        <col style="width: 15%">
                        <col style="width: 15%">
                        <col style="width: 15%">
                    </colgroup>
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Name</th>
                            <th>Type</th>
                            <th>Color</th>
                            <th>Created At</th>
                            <th>Updated At</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody id="configuration-list">
                        {% if configurationsPager|length > 0 %}
                            {% for configuration in configurationsPager %}
                                {{ component('ConfigurationRow', {
                                    configuration: configuration,
                                    targetModalId: modal.id
                                }) }}
                            {% endfor %}
                        {% else %}
                            <tr>
                                <td colspan="7" class="text-center">
                                    No configurations found.
                                </td>
                            </tr>
                        {% endif %}
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="7" class="text-center">
                                <a class="btn btn-success" href="{{ path('app_configuration_type') }}">
                                    Create a new entry
                                </a>
                            </td>
                        </tr>
                    </tfoot>
                </table>
            </div>
            <div class="col-md-12">
                <div class="d-flex justify-content-between align-items-center">
                    <a class="btn btn-lg btn-primary" href="{{ path('app_index_index') }}">
                        Home
                    </a>
                    {% if configurationsPager.hasNextPage or configurationsPager.hasPreviousPage %}
                        {{ pagerfanta(configurationsPager) }}
                    {% endif %}
                    <a class="btn btn-lg btn-primary" href="{{ path('app_user_index') }}">
                        Profile
                    </a>
                </div>
            </div>
            </turbo-frame>
        </div>
    </div>
    {{ component('ConfirmModal', modal) }}
{% endblock %}
